<?php
header("Content-type: text/json");

$feedxml = simplexml_load_file("https://www.ifsr.de/_feed/fsr:ticker");
$tickerxml = $feedxml->xpath("/rss/channel/item/description");

foreach($tickerxml as $k=>$v) {
		$ticker = $v->__toString();
		$ticker = str_replace(array("<p>","</p>"), "", $ticker);
		$ticker = str_replace("&quot;", "\"", $ticker);
		echo $ticker;
		echo "<!--next-->";
	}
?>
