# Installer config for raspbian
# see https://github.com/debian-pi/raspbian-ua-netinst#installer-customization
preset=server
release=wheezy
packages=chromium,nano,screen,curl,omxplayer,lighttpd,lightdm,git,raspi-config,sudo,xmlstarlet,xserver-xorg,xinit,xserver-xorg-video-fbdev,snmp,sudo,vim,apt-utils,unzip,gcc,make,libssl-dev,zlib1g-dev,lsb-release,libffi-dev,xdotool
hostname=infoscreen
domainname=ifsr.de
rootpw=fsrberry
online_config= # URL to extra config that will be executed after installer-config.txt
usbroot= # set to 1 to install to first USB disk
